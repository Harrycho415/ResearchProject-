import { useRouter } from 'next/router';
import React from 'react';

export default function Card() {
  const router = useRouter();

  return (
    <div className=" ">
      <div className=" mx-auto text-black">Hello</div>
    </div>
  );
}
